name: Main workflow

on:
  push:
  pull_request:
    
jobs:
  install:
    uses: Pcrab/pblive-lib/.github/workflows/prepare.yml@main

  build:
    uses: Pcrab/pblive-lib/.github/workflows/build.yml@main
    needs: [install]

  lint:
    uses: Pcrab/pblive-lib/.github/workflows/lint.yml@main
    needs: [install]

  test:
    uses: Pcrab/pblive-lib/.github/workflows/test.yml@main
    needs: [install]
